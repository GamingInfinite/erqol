//! Gameplay tweaks that are broader QoL behaviour changes, kept separate from
//! `qol` (which is mostly navigation / UI conveniences). Currently home to the
//! custom map icons patch and the summon-spirits-anywhere tweak.

pub mod map_icons;
pub mod spirit_summon;
